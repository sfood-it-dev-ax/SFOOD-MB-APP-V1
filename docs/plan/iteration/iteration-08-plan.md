# Iteration 08 Plan

## Goal
Implement board move API and make frontend requirement validation pass for board move flow.

## Scope
- Add board move request DTO and endpoint.
- Extend board domain with parent/sort fields.
- Implement move validation (self-parent and cycle prevention).
- Update integration tests for move endpoint.
- Run `./gradlew test` and `./gradlew build`.
